# SatQuery AI — Frontend Safe Environment Configuration
# API keys and provider secrets are NEVER placed in frontend environment variables.

# Backend API Base URL (leave empty for same-origin proxy '/api/v1')
VITE_API_BASE_URL=

# Force Mock Demo API (true = offline demo mode, false = real FastAPI backend)
VITE_USE_MOCK_API=false
